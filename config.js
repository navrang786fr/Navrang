/* Navarang order-app configuration — edit these before launch. */
var RESTAURANT_CONFIG = {
  name: "Navarang",
  /* WhatsApp number that receives orders, in international format with no
     leading +, spaces or dashes (country code + number). Example for an
     Indian number +91 98765 43210 → "919876543210". Orders will fail to
     send until this is set to your restaurant's real WhatsApp number. */
  whatsappNumber: "910000000000",
  currency: "₹"
};
